# Arquivo principal, se quiser pode ficar vazio ou
# usar para organizar seu projeto.
terraform {
  required_version = ">= 1.0.0"
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 4.0"
    }
  }
}
